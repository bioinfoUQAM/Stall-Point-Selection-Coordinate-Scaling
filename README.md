# Stall-Point-Selection-Coordinate-Scaling
Tool to interactively select the 4 corners of an animal stall on the first video frame, then normalize bounding box coordinates relative to the stall geometry.
